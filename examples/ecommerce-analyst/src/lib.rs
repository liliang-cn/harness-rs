//! Shared library for the ecommerce-analyst example: database schema +
//! realistic seed (`db`) and the read-only SQL tool (`sqltool`) the analyst
//! agents use to query the live store.

pub mod db;
pub mod sqltool;
