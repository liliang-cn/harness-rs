/target
/target-linux
/target-musl
/HARNESS_NOTES.md
**/Cargo.lock.bak
.env
.env.local
# Playwright MCP test artifacts
.playwright-mcp/
# Ad-hoc UI screenshots from manual testing
/*.png
# admin-ui: node_modules never in git; dist/ IS committed so `cargo build`
# works without first running `npm run build` (the binary embeds dist via
# include_dir!).
examples/ai-ledger/admin-ui/node_modules/
# macOS Finder metadata
.DS_Store
**/.DS_Store
# CortexDB local SQLite store (per-machine, not source)
cortexdb.db
cortexdb.db-shm
cortexdb.db-wal
# superpowers tooling cache
.superpowers/
# stray npm lockfile at repo root (this is a Cargo workspace, not npm);
# example UIs (examples/ai-note/*) keep their own committed lockfiles
/package-lock.json
# local article/export output
/out/
